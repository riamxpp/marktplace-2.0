import styled from "styled-components";

const Button = styled.button`
  padding: 0.1rem 0.3rem;
  width: 1rem;
  font-weight: bold;
  color: #fff;
  background-color: #3337de;
  border: none;
  cursor: pointer;
`;

export default Button;
