import styled from "styled-components";

const Loja = styled.article`
  width: 95%;
  height: 4rem;
  padding: 1rem;
  border-radius: 4px;
  box-shadow: 4px 4px 4px rgba(0, 0, 0, 0.7);
  background: #fff;
  display: flex;
`;

export default Loja;
