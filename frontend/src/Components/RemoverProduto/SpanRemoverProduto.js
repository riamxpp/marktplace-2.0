import styled from "styled-components";

const SpanRemoverProduto = styled.span`
  color: ${({ color }) => color};
  font-weight: 500;
  font-size: 0.8rem;
  line-height: 1.5;
  display: block;
  cursor: pointer;
`;

export default SpanRemoverProduto;
