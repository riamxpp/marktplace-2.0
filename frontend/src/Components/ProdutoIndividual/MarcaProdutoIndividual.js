import styled from "styled-components";

const MarcaProdutoIndividual = styled.p`
  text-transform: uppercase;
  font-size: 1rem;
  font-weight: bold;
  line-height: 1.5;
`;

export default MarcaProdutoIndividual;
