import styled from "styled-components";

const ProdutoSeusProdutos = styled.article`
  width: 14rem;
  height: auto;
  box-shadow: 1px 0px 13px rgb(0 0 0 / 70%);
  padding: 1rem;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border-radius: 2px;
`;

export default ProdutoSeusProdutos;
