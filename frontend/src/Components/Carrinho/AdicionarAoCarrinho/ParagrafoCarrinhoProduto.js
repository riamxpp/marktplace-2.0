import styled from "styled-components";

const ParagrafoCarrinhoProduto = styled.p`
  font-weight: ${({ weigth }) => weigth};
  font-size: ${({ size }) => size};
`;

export default ParagrafoCarrinhoProduto;
